services:
  web:
    image: cevaude/foodmood:latest
    ports:
      - "${PROD_PORT:-8000}:8000"
    restart: always
    environment: []
    volumes:
      - db_foodmood:/var/lib/sqlite/data/

volumes:
  db_foodmood:
